<?xml version="1.0" encoding="UTF-8"?>
<!-- 
    POM (Project Object Model) file for Maven
    Maven is a build automation tool used for Java projects
    It manages project dependencies, builds, and packaging
-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 
         http://maven.apache.org/xsd/maven-4.0.0.xsd">
    
    <!-- Defines the POM model version -->
    <modelVersion>4.0.0</modelVersion>
    
    <!-- 
        Parent POM: Inherits from Spring Boot starter parent
        This provides default configurations and dependency management
    -->
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.1.5</version>
        <relativePath/>
    </parent>
    
    <!-- Project coordinates - uniquely identifies this project -->
    <groupId>com.bookstore</groupId>         <!-- Company/organization domain -->
    <artifactId>online-bookstore</artifactId> <!-- Project name -->
    <version>1.0.0</version>                  <!-- Project version -->
    <name>Online Bookstore</name>             <!-- Human-readable name -->
    <description>Spring Boot Online Bookstore Application</description>
    
    <!-- Java version configuration -->
    <properties>
        <java.version>17</java.version>
    </properties>
    
    <!-- 
        Dependencies: External libraries needed for the project
        Spring Boot starter dependencies come with pre-configured settings
    -->
    <dependencies>
        
        <!-- 
            Spring Boot Web Starter
            Includes: Spring MVC, REST, Tomcat (embedded server), Jackson (JSON)
            Used for: Building web applications and RESTful services
        -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        
        <!-- 
            Spring Boot Thymeleaf Starter
            Thymeleaf is a template engine for rendering HTML pages
            Used for: Creating dynamic HTML views on server-side
        -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-thymeleaf</artifactId>
        </dependency>
        
        <!-- 
            Spring Boot Data JPA Starter
            JPA (Java Persistence API) for database operations
            Includes: Hibernate ORM, Spring Data JPA
            Used for: Database CRUD operations with object-relational mapping
        -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>
        
        <!-- 
            Spring Boot Security Starter
            Provides authentication and authorization
            Used for: User login, password encryption, securing endpoints
        -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>
        
        <!-- 
            MySQL Connector
            JDBC driver for MySQL database
            Used for: Connecting Spring Boot application to MySQL database
        -->
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <scope>runtime</scope> <!-- Only needed at runtime, not compile time -->
        </dependency>
        
        <!-- 
            Spring Boot Validation Starter
            Bean Validation with Hibernate validator
            Used for: Form validation (email, required fields, etc.)
        -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
        
        <!-- 
            Lombok
            Reduces boilerplate code (getters, setters, constructors)
            Used for: Automatic generation of common methods using annotations
        -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>
        
        <!-- 
            Spring Boot DevTools
            Provides fast application restarts and live reload
            Used for: Development - automatic restart when code changes
        -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-devtools</artifactId>
            <scope>runtime</scope>
            <optional>true</optional>
        </dependency>
        
    </dependencies>
    
    <!-- 
        Build configuration
        Maven plugins for building the application
    -->
    <build>
        <plugins>
            <!-- 
                Spring Boot Maven Plugin
                Used for: Creating executable JAR, running the application
            -->
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                </configuration>
            </plugin>
        </plugins>
    </build>
    
</project>
